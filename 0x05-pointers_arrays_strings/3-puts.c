#include "main.h"
/**
 * _puts - prints a string,followed by a new line to stdout
 * @str:string to be printed
 */
void _puts(char *str)
{
int = 0;
*str = i;
for (i = 0; *str[i] != '\0'; i++)
{
_putchar(*str[i]);
}
_putchar('\n');
}
